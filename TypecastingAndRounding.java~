public class TypecastingAndRounding {
    public static void main(String[] args) { 
        //typecasting 
        int i = 2;
        double d = 3.7;
        int x;
        x = i * (int)d;
        System.out.println(x);
        
        //rounding
        x = i*(int)(d + 0.5);
        System.out.println(x);
        /* A number with a decimal portion greater thn or equal to 0.5 should be rounded up to the next integer, 
         * and a number with a decimal portion less than 0.5 should be rounded down. 
         * Basically, in the code above, if 'd' has a decimal portion more than 0.5, when we add 0.5, it should change
         * the value of the 1's place number before it gets truncated. If 'd' has a decimal portion of less than 0.5. 
         * when adding 0.5, the number in the one's place stays the same before truncating.
         */
    }
}
